# Lab0 — Installation cluster Hadoop (Docker)

- Lancer: `docker compose up -d` puis `docker exec -it hadoop-master bash && ./start-hadoop.sh`
- UIs utiles: 9870 (NameNode), 8088 (YARN), 19888 (History).
- Créez `/user/root` avant d'utiliser HDFS.
